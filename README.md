# A very simple bloop build

* bootstraps Bloop via Coursier
* supports IntelliJ import via Build Server Protocol via bloop
* works on Linux/Mac

## Instructions

1. run ./init.sh
2. import into IntelliJ as bsp
3. compiling should work
